# Used only when running from a cloned copy of the repo (e.g. `uv run cyberdefense`).
# For a normal `uv tool install`, the app will ask you for this key on first run
# and save it to ~/.cyberdefense/.env instead - you don't need this file.
VIRUS_TOTAL_API=your_virustotal_api_key_here
